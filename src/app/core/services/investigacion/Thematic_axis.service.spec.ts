/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Thematic_axisService } from './Thematic_axis.service';

describe('Service: Thematic_axis', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Thematic_axisService]
    });
  });

  it('should ...', inject([Thematic_axisService], (service: Thematic_axisService) => {
    expect(service).toBeTruthy();
  }));
});

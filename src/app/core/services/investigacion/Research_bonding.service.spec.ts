/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Research_bondingService } from './Research_bonding.service';

describe('Service: Research_bonding', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Research_bondingService]
    });
  });

  it('should ...', inject([Research_bondingService], (service: Research_bondingService) => {
    expect(service).toBeTruthy();
  }));
});
